While edition 4 of Agile Web Development for Rails was intended to cover
Rails 3.* generically, Rails itself made a large number of user facing changes
between Rails 3.0 and Rails 3.1.  This directory contains the complete source
code for code samples that work with both versions.

The rails30 directory contain samples designed to work with Rails 3.0.5.

The rails31 directory contain samples designed to work with Rails 3.1.0.

-----

『RailsによるアジャイルWebアプリケーション開発 第4版』では Rails 3 全般を解説していますが、
Rails 3.0 と Rails 3.1 には利用者に見える大きな違いがいくつかあります。
このディレクトリには、Rails 3.0 および Rails 3.1 用のサンプルコードがそれぞれ含まれています。

ディレクトリ rails30 には、Rails 3.0.5 で動作するコードが含まれています。

ディレクトリ rails31 には、Rails 3.1.0 で動作するコードが含まれています。

